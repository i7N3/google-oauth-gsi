import GoogleOAuthProvider from '../src/main';

describe('test case', () => {
    it('5 equals to 5', () => {
        // const provider = new GoogleOAuthProvider({
        //     clientId: '<GOOGLE_CLIENT_ID>',
        // });
        expect(5).toEqual(5);
    });
});
